[build]
  base = "web"
  command = "bun install && NODE_OPTIONS='--no-warnings' bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  BUN_VERSION = "1.2.4"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
